# 👑 Royal Discord Bot

A royal-themed Discord bot built with **Discord.js v14**.

## ✨ Features
- Prefix commands (`.`)
- Slash commands
- Moderation tools
- Level & message tracking
- Global broadcast system
- Clean & modular structure

## 📦 Requirements
- Node.js v18+
- Discord Bot Token

## ⚙️ Setup
1. Clone the repo
2. Install dependencies:
   ```bash
   npm install
   
3. Create a .env file:
```DISCORD_BOT_TOKEN=YOUR_TOKEN
DISCORD_BOT_PREFIX=.
OFFICIAL_SERVER_ID=YOUR_SERVER_ID
OFFICIAL_CHANNEL_ID=YOUR_CHANNEL_ID
4. Start the bot:
```npm start

# 👑 Author
Built with ❤️ by Royal Bot Team

---

## 🔐 SECURITY FINAL CHECK (IMPORTANT)

Make sure:
- ❌ `.env` is **NOT** in the repo
- ✅ `.env.example` **IS**
- ❌ `node_modules` not uploaded
- ✅ `package.json` & `package-lock.json` uploaded

---
